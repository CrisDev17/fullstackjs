.users
form.seach-form(action="/users/search", method="POST")
  input(name="name", type="text", placeholder="name")
  input(type="submit")

  table
    thead
      tr
        th  Name 
        th  Email 
    tbody
    each user in users
      tr
        td=  user.name 
        td=  user.email
