import url from '../config'
export default {
   //  getUsers({commit}){                        //context đại diện cho store hiện tại , chỗ này destructring ra được { state , commit}
   //      fetch(url.USER_API_URL)
   //      .then( response => {
   //         return response.json()
   //      })
   //      .then(users => {
   //         commit('setUsers',users)
   //      })
   //    }
   getUser({commit}){
      
   }
    }